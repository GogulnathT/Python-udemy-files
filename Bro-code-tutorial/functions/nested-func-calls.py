#nested function call = function calls within function calls 
#                       innermost function calls are resolved first and then the return or output is
#                       used for the next function

print(round(abs(float(input("Enter a decimal number to be rounded off: ")))))